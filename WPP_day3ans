let number = Math.ceil(Math.random() * 10);
console.log(number);
let guessnumber = prompt("Guess number between 1 to 10: ");
if(guessnumber == number) {
    console.log("Good work");
} else{
    console.log("Not matched");
}
