function sumarray(arr) {  
    let sum = 0;  
    for (let i = 0; i < arr.length; i++)  
    sum += arr[i];  
    return sum;  
    }

let arr = [12, 3, 4, 15];
console.log("Sum of given array is " + sumarray(arr));  
